package com.example.appandoid.providers

import android.content.Context
import com.example.appandoid.R
import com.example.appandoid.models.homeModels.DestinationModel
import com.example.appandoid.models.homeModels.OfferModel

/*class OfferProvider (private val context: Context) {

        companion object {
            fun getOffersList(context: Context): List<OfferModel> {
                val offersList = mutableListOf<OfferModel>()

                offersList.add(
                    OfferModel(
                        "20% discount",
                        "for",
                        "mastercard users",
                        "Limited time offer!")
                       // getDrawableResourceName(context, R.drawable.offer_mastercard)

                )

                offersList.add(
                    OfferModel(
                        "25% discount",
                        "with",
                        "your Visa credit cards",
                        "Limited time offer!")
                       // getDrawableResourceName(context, R.drawable.offer_visa)

                )
                return offersList
            }

            private fun getDrawableResourceName(context: Context, drawableId: Int): String {
                return context.resources.getResourceName(drawableId)
            }
        }
    }*/
   /* companion object{
        val offersList = listOf<OfferModel>(
            OfferModel(
                "20% discount",
                "for",
                "mastercard users",
                "Limited time offer!",
                ""),

            OfferModel(
                "25% discount",
                "with",
                "your Visa credit cards",
                "Limited time offer!",
                "")

        )
        private fun getDrawableResourceName(drawableId: Int): String {
            val context = conte // Get context from activity or fragment
            return context.resources.getResourceName(drawableId)
        }

    }*/
